# Runtime deps (resolved + locked in the Docker build on linux/amd64).
# Versions are floored to known-good; exact pins are produced at image build.
fastapi>=0.115,<1
uvicorn[standard]>=0.32,<1
insightface==0.7.3
onnxruntime>=1.20,<2
numpy>=1.26,<2
pillow>=10,<12
httpx>=0.27,<1
python-multipart>=0.0.9
